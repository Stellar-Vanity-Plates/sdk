/**
 * Shared Colibri APIs for Vanity Plates SDK consumers.
 * Import from @vanity-plates/sdk/colibri to use the SDK's Colibri dependency.
 * These are the original implementations and retain their constructor identity.
 * @module
 */
export {
  ColibriError,
  Contract,
  LocalSigner,
  NetworkConfig,
  SorobanType,
  Spec,
  StrKey,
} from "@colibri/core";
export type {
  AuthEntrySigner,
  ContractConfig,
  ContractConstructorArgs,
  ContractErrorMap,
  ContractId,
  Ed25519PublicKey,
  EnvelopeSigner,
  KeypairSigner,
  Signer,
  TransactionConfig,
} from "@colibri/core";
